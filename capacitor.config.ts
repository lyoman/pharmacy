import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'www.pharmacy.locator',
  appName: 'pharmacy',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
